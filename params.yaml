params_yaml = """
# params.yaml – Hyperparamètres du pipeline
preprocess:
  knn_neighbors: 5
  iqr_factor: 3.0
  max_features_tfidf: 10000
  ngram_range: [1, 2]

train:
  seed: 42
  test_size: 0.30
  val_size: 0.50
  cv_folds: 5

clf:
  C: 5.0
  max_iter: 1000

reg:
  n_estimators: 200
  max_depth: 20

cluster:
  k_range: [2, 12]
  n_init: 20
"""

with open('params.yaml', 'w') as f:
    f.write(params_yaml)
print('params.yaml cree')